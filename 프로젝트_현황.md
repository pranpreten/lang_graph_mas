# 프로젝트 현황 정리 (이어서 작업용)

> 스마트 제조 처방적 유지보수(RxM)를 위한 **LLM 멀티에이전트 조율 수준(L1~L4) 비교** 실험.
> C-MAPSS(터보팬 FD001) 데이터. LangGraph 기반. 커맨더=Claude Opus 4.8, SLM=Ollama(qwen3:4b).

---

## 1. 핵심 설계 결정

- **무게중심: 성능 → 안정성·비용.** 단, 교수님 피드백 반영해 **성능도 트레이드오프 축으로 같이 본다.**
- **데이터: C-MAPSS FD001** (터보팬 열화). 입력 X(센서24개)는 3태스크 공통, **라벨 y만 다르게 파생**:
  - 회귀: 잔여수명(RUL) 예측 (R²) — 보조: RMSE·MAE
  - 분류: 30사이클 내 고장? (F1) — 보조: precision·recall·accuracy
  - 이상탐지: 비지도, 정상(초기30사이클)만 학습 (AUC) — 보조: PR-AUC
- **조율 L1~L4** = 커맨더 개입량:
  - L1: route(분배)만 ≈1회
  - L2: + 단계별 review(검토) ≈5회
  - L3: + 단계별 guide(가이드, SLM 프롬프트에 주입) ≈9회
  - L4: + control_ml(모델 직접 지정) **+ 하이퍼파라미터 그리드서치 튜닝** ≈12회
- **레벨이 결과를 바꾸는 경로**: 전처리 선택 / 모델 선택 / (L4)HP 튜닝. L1=L2는 점수 동일(검토는 통과·재시도만), L3는 가이드로, L4는 모델+HP로 달라질 수 있음.
- **검토 2층**: ①객관 검증(점수 임계·에러) + ②커맨더 LLM 판단. 임계: F1<0.5 / R²<0.5 / AUC<0.6.
- **공정성**: 모델 후보군 전 레벨 공통, L4 HP튜닝도 HP_SEARCH_SPACE 안에서만(모델·범위 발명 금지).
- **반복 3회 → 방식 A = 4레벨 × 3태스크 × 3 = 36런.** 비용 1차 지표 = 호출수·토큰(시간 보조).
- **방식 B(LLM 위임)는 보류** — 36런 트레이드오프 결과 보고 진행 여부 결정.

## 2. 파일 구조 (rxm_experiment/)

```
config.py        설정·임계값·정책 (SLM_MODEL=qwen3:4b, N_REPEATS=3, MAX_RETRIES=1, HP_SEARCH_SPACE)
prompts.py       시스템 프롬프트
state.py         공유 상태
llm.py           Claude+Ollama 호출 + trace + 타임아웃 + 시간측정(TIMING)
graph.py         LangGraph 조립 (L1~L4 레벨별 분리)
logger.py        런 요약 → logs/runs.jsonl (commander_sec·slm_sec·전처리·tuned 포함)
runner.py        배치 실행 (콘솔에 커맨더/SLM 시간·재시도 단계 표시)
analyze.py       집계 → 안정성·비용 표 + results/*.csv
evaluate.py      트레이드오프 그래프(PNG) + 태스크별 최적레벨 판정 + 평가_분석.md  ★신규
run_experiment.py  한 방 실행 (runner+analyze, --fresh)
view_trace.py    한 런 대화 흐름 보기
nodes/  commander · perception · preprocessing · analysis(HP튜닝) · prescription · _parse · _ctx
data/   data_prep.py + train/test/RUL_FD001.txt
```

## 3. 현재 상태

- **코드 완성 + 실제 서버 실행 단계** (euclidsoft, A100). qwen3:4b가 GPU 100% 정상 점유.
- 방식 A 36런 돌리는 중 / 돌릴 준비 완료.

## 4. 이번 세션에서 바뀐 것 (changelog)

1. **SLM 8b→4b 전환** 서버 반영 (8b는 부분로드로 느림·hang). config·llm 잘림 복구 포함.
2. **qwen3 thinking 끄기** — `/no_think`를 system·user 양쪽 주입 (think=False 무시하는 서버 대비). 런당 130~175s → 단축. + num_predict 768 상한.
3. **반복 10→3** (N_REPEATS), **재시도 2→1** (MAX_RETRIES; 결정론 ML이라 재시도 무의미).
4. **재시도 폭주 버그 수정** — commander.py의 perception 검토 스냅샷이 'task명' 한 글자만 넘겨서 커맨더가 계속 "재시도" 시킴 → 실제 data_summary 내용(피처수·통계·저분산)을 넘기도록 수정.
5. **score_detail 보조지표 추가** — 분류 P/R/Acc, 회귀 MAE, 이상탐지 PR-AUC.
6. **L4 하이퍼파라미터 튜닝 구현** (analysis.py tune_hp, GridSearchCV n_jobs=-1) — 슬라이드 "파라미터까지 직접 결정"의 실제 구현. 조율이 성능에도 영향 주도록. 이상탐지는 비지도라 스킵.
7. **로깅 강화** — runs.jsonl에 commander_sec·slm_sec(시간 분해)·전처리(features_mode·scaler)·tuned. trace.jsonl에 호출별 dur_sec.
8. **evaluate.py 신규** — x=성능 y=비용 트레이드오프 그래프 + 태스크별 비용대비 최적레벨 자동판정 + 서술 리포트.
9. **analyze.py 강화** — 표에 전처리일관성·평균재시도·SLM호출·커맨더초·SLM초 추가.
10. 슬라이드 `방법및구현.html` (상위폴더) — LangGraph 기반, 커맨더=Claude Opus 4.8, SLM=Qwen3:4B로 수정.

## 5. 다음 할 일

1. (서버) `git pull` 후 `python run_experiment.py` → 36런
2. (서버) 결과 가져오기: `git add -f logs/runs.jsonl` + results CSV → push  (※ logs/는 .gitignore 대상이라 -f 필요. trace.jsonl은 무거워 제외)
3. (로컬) `git pull` → `python evaluate.py` (+ `--cost tokens` 등) + `python analyze.py`
4. 트레이드오프 그래프·표 보고 → **방식 B 진행 여부 + 교수님께 보고**

## 6. 알려진 미해결 / 점검거리

- L1 일부 태스크 완주 실패(분류·이상탐지) — 낮은 조율의 불안정성 증거이자 점검거리. 재발 시 `view_trace.py`로 확인.
- L4 HP튜닝이 이 데이터에서 점수를 얼마나 움직이는지는 36런 결과로 확인 필요.
- 성능이 레벨 간 거의 동일하면 → 안정성(완주율·일관성)·비용 축으로 태스크별 차별화 서술.

## 7. 자주 쓰는 명령어

```bash
python run_experiment.py        # 36런 본실험 + 집계
python evaluate.py              # 트레이드오프 그래프 + 태스크별 최적레벨 + 분석 md
python evaluate.py --cost tokens   # 비용축 바꾸기
python analyze.py               # 안정성·비용 표
python view_trace.py <run_id>   # 한 런 대화 흐름
```

## 8. 교수님 피드백 (2026-06-14) & 반영

- "비용 vs 성능은 트레이드오프 → x=성능 y=비용 2차원 그래프로 최적 구간 시각화" → **evaluate.py로 구현**
- "L3가 다 좋다 식 말고, 분류는 L2면 충분·이상탐지는 L4 필수처럼 태스크 복잡도별 차이 입증" → 태스크별 최적레벨 자동판정 + (성능 평탄 시) 안정성으로 차별화
