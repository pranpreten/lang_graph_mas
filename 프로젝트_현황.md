# 프로젝트 현황 정리 (이어서 작업용)

> 스마트 제조 처방적 유지보수(RxM)를 위한 **LLM 멀티에이전트 조율 수준(L1~L4) 비교** 실험.
> C-MAPSS(터보팬) 데이터. LangGraph 기반. 커맨더=Claude Opus 4.8, SLM=Ollama(qwen3).

---

## 1. 핵심 설계 결정 (대화하며 정한 것)

- **무게중심: 성능 → 안정성·비용.** (성능 F1/R²/AUC는 보조 지표. 성능은 모델 선택에 좌우돼 조율 효과를 깨끗이 못 봄)
- **데이터: C-MAPSS FD001** (터보팬 엔진 열화). AI4I에서 변경.
- **3 태스크 = 3 ML 패러다임**:
  - 회귀: 잔여수명(RUL) 예측 (R²)
  - 분류: 30사이클 내 고장? (F1) — RUL≤30을 라벨로 직접 생성
  - 이상탐지: 비지도 (AUC) — 정상 구간만 학습
- **조율 L1~L4** = 커맨더 개입량:
  - L1: route(분배)만 ≈1회
  - L2: + 단계별 review(검토) ≈5회
  - L3: + 단계별 guide(가이드) ≈9회
  - L4: + 분석에 control_ml(모델 직접 지정) ≈10회+
- **검토 2층**: ①객관 검증(점수 임계·에러, 코드) + ②LLM 판단. 점수 임계: F1<0.5 / R²<0.5 / AUC<0.6 재시도.
- **처방**: 임계치 고정(RUL 30/125 등) + SLM이 근거 서술(추론형). 임계치는 SLM이 안 정함.
- **방식 B(LLM 위임)는 보류**: 안정성·비용 프레이밍에선 의미 약함. **방식 A 결과(최적 레벨이 태스크마다 갈리나?) 보고 결정.**
- 반복 10회 → **방식 A = 4레벨 × 3태스크 × 10 = 120런**. 비용 1차 지표 = 토큰·호출수(시간은 보조).

## 2. 파일 구조 (rxm_experiment/)

```
config.py        설정·요청문장·임계값·정책 (SLM_MODEL, QUALITY_THRESHOLDS 등)
prompts.py       시스템 프롬프트 8개 (전 레벨 고정)
state.py         공유 상태(State)
llm.py           Claude+Ollama 호출 + trace 기록 + 타임아웃
graph.py         LangGraph 조립 (build_graph_L1~L4, 레벨별 분리)
logger.py        런 요약 → logs/runs.jsonl
runner.py        배치 실행 (--pilot, --fresh)
analyze.py       집계 → 안정성·비용 트레이드오프 + results/*.csv
run_experiment.py  한 방 실행 (runner+analyze, 항상 --fresh)
view_trace.py    한 런 대화 흐름 보기
verify_api.py    커맨더 연결 확인
nodes/  commander · perception · preprocessing · analysis · prescription · _parse · _ctx
data/   data_prep.py + train/test/RUL_FD001.txt
참고: requirements.txt, README.md, 실행가이드.md, .env(키), 코드흐름_L1-L4.html
```

## 3. 현재 상태

- **코드 100% 완성** (방식 A 전체 + 로깅 + trace + 집계). 컴파일·가짜데이터 검증 완료.
- **서버에서 실제 실행 테스트 중** (euclidsoft 서버, A100×5).
- 실제 LLM(Claude)·SLM(Ollama)·sklearn으로 도는 것 확인됨 (L1~L2 일부 런 성공).

## 4. 서버 GPU 이슈 & 적용한 해결

- A100 5장인데 Ollama가 GPU를 못 잡음 → 원인: CUDA 13.1 드라이버에 Ollama CUDA 러너가 GGML device_count assert로 크래시.
  - **해결**: `sudo systemctl edit ollama` → `Environment="CUDA_VISIBLE_DEVICES=0"` → restart. (GPU 일부 사용 시작)
- num_gpu=99(전체 오프로드)는 OOM (MIG 슬라이스로 실사용 메모리 작은 듯) → **num_gpu 제거 + num_ctx 8192→4096**.
- 한 런 ~45초 + 6번째 런에서 무한 hang → **SLM을 qwen3:8b→qwen3:4b** (슬라이스에 통째로 올라가 빠름) + **llm.py에 타임아웃**(커맨더 120s/SLM 180s, 멈춰도 실패 처리하고 다음 진행).

## 5. 다음 할 일 (서버에서)

1. **변경된 config.py · llm.py를 서버에 재반영** (git pull 또는 복사) ← 위 GPU 수정이 워크스페이스에만 돼있음
2. `ollama pull qwen3:4b`
3. **tmux(또는 nohup)로 실행** — SSH 끊겨도 계속 돌게:
   ```bash
   tmux new -s exp          # 세션 생성
   python run_experiment.py pilot
   #   (나갈 때 Ctrl+B 누르고 D → detach. 다시 들어올 때: tmux attach -t exp)
   ```
4. pilot(12런) 잘 되면 → `python run_experiment.py` (120런)
5. `python analyze.py` 로 결과 확인
6. (방식 A 결과 보고) 방식 B 할지 + 교수님께 프레이밍 변경 피드백

## 6. 알려진 미해결 / 점검거리

- **L1 anomaly 런이 분석 실패(완주=False)** 난 적 있음 → `python view_trace.py L1-anomaly-r0`로 원인 확인 필요 (이상탐지 분석 노드 디버깅).
- GPU 100% 못 씀 (부분 GPU/4b로 우회 중). 근본 해결하려면 관리자에게 Ollama+CUDA13 호환 또는 MIG 설정 문의.

## 7. 자주 쓰는 명령어

```bash
python graph.py L1 regression --run     # 단독 1번 (구경)
python run_experiment.py pilot          # 12런 점검 + 집계
python run_experiment.py                # 120런 본실험 + 집계
python analyze.py                       # 결과표 다시 보기
python view_trace.py                    # 런 목록 → run_id 골라 자세히
```
