# 실행 가이드

> 먼저: `cd rxm_experiment` + venv 켜기 (`source .venv/bin/activate`)

## 실험 돌리기
```bash
python run_experiment.py pilot     # 점검용 (12번, 짧게)
python run_experiment.py           # 본실험 (120번)
```
→ 끝나면 결과표가 자동으로 출력됨.

## 결과 다시 보기
```bash
python analyze.py                  # 레벨별 안정성·비용 요약표
```

## 한 판 대화 구경 (선택)
```bash
python view_trace.py               # 어떤 런 있는지 목록
python view_trace.py L1-regression-r0   # 그 런에서 에이전트가 뭐라 했는지
```

---
끝. 보통 `run_experiment.py pilot` → 잘 되면 `run_experiment.py` 이거면 됨.
